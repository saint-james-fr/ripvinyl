<div class="container mt-5">
  <%= render "releases/loading_overlay" %>
  <div id="anchor_page_up"></div>
  <%= render "releases/filter_options", most_collected_styles: @most_collected_styles%>
  <%= render "releases/pagination", releases: @releases%>
  <%= render "releases/releases_gallery", releases: @releases %>
  <div class="mx-auto m-3 col-4 d-flex justify-content-center">
    <button class="btn btn-info">Back to the top</button>
  </div>
  <%= render "releases/pagination", releases: @releases%>
</div>
